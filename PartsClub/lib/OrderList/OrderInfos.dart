class OrderInfos{
  String deliveryAddress, orderDate, orderNo, orderStatus, paymentMethod, deliveryDate;
  double totalAmount;
  OrderInfos(this.deliveryAddress, this.orderDate, this.orderNo, this.orderStatus, this.paymentMethod, this.deliveryDate, this.totalAmount);
}